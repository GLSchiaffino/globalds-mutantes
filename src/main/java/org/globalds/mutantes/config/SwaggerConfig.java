package org.globalds.mutantes.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // SpringDoc se configura solo, esto queda vacío.
}
